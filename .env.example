# Copie este arquivo para ".env" e preencha com os valores reais do seu ambiente.
# O arquivo ".env" nunca deve ser commitado (já está no .gitignore).

DB_HOST=localhost
DB_NAME=patrimonio
DB_USER=root
DB_PASS=
